class AuthorPost {
    constructor(authorTitle, postId, authorBody) {
        this.authorTitle = authorTitle;
        this.postId = postId;
        this.authorBody = authorBody;
    }
}

export default AuthorPost;